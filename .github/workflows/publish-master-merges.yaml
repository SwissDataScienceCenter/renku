name: Publish all merges to master

on:
  push:
    branches: ["master"]
    paths:
      - helm-chart/**
      - scripts/**
      - acceptance-tests/**
      - .github/**
  workflow_dispatch:

jobs:
  publish-chart:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: azure/setup-helm@v1
        with:
          version: '3.8.0'
        id: setup-helm
      - id: get-latest-helm-charts
        run: |
          helm repo add renku https://swissdatasciencecenter.github.io/helm-charts/
          helm repo update
          helm search repo renku --devel -o yaml > .latest-charts.yaml
      - id: get-latest-version
        uses: mikefarah/yq@v4.19.1
        with:
          cmd: yq '.[] | select(.name == "renku/renku") | .version' .latest-charts.yaml | cut -d'.' -f1-3
      - uses: actions-ecosystem/action-bump-semver@v1
        id: bump-semver
        with:
          current_version: ${{ steps.get-latest-version.outputs.result }}
          level: prerelease
      - id: set-version
        run: |
          echo "publish_version=${{ steps.bump-semver.outputs.new_version }}.$(echo ${{ github.sha }} | cut -c 1-7)" >> $GITHUB_ENV
          rm .latest-charts.yaml
      - uses: SwissDataScienceCenter/renku-actions/publish-chart@v0.5.0
        env:
          CHART_DIR: helm-chart/
          CHART_TAG: "--tag ${{env.publish_version}}"
          CHART_NAME: renku
          GIT_USER: renku-bot
          GIT_EMAIL: renku@datascience.ch
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          GITHUB_TOKEN: ${{ secrets.RENKUBOT_GITHUB_TOKEN }}
      - uses: 8398a7/action-slack@v3
        with:
          status: custom
          fields: job,ref
          custom_payload: |
            {
              attachments: [{
                color: '${{ job.status }}' === 'success' ? 'good' : '${{ job.status }}' === 'failure' ? 'danger' : 'warning',
                text: `${process.env.AS_JOB} version ${{ env.publish_version }}: ${{ job.status }}.`,
              }]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        if: always()
      - uses: actions-ecosystem/action-push-tag@v1
        with:
          tag: ${{ env.publish_version }}
          message:
            "${{ env.publish_version }}: PR #${{
            github.event.pull_request.number }} ${{
            github.event.pull_request.title }}"
