type: object
properties:
  id:
    type: string
    description: uuid of the vertex type.
  name:
    type: string
    description: The name of the vertex type.
  properties:
    type: array
    items:
      $ref: '../typesystem.yaml#/definitions/PropertyKey'
  domain:
    $ref: '../typesystem.yaml#/definitions/GraphDomain'
