parameters:
  - name: idname
    in: path
    description: the name or the uuid of the graph domain
    type: string
    required: true
get:
  summary: Return the given graph domain
  description: Return the given graph domain
  responses:
    200:
      description: Returns the given graph domain
      schema:
        $ref: ../../../api/definitions.yaml#/definitions/MgmtGraphDomain
    default:
      description: Unexpected error
      schema:
        $ref: ../../../api/definitions.yaml#/definitions/Error