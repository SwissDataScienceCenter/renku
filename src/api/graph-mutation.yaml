# API spec in YAML
swagger: '2.0'
info:
  title: Graph Mutation Service
  description: Mutate the graph
  version: "1.0.0"
# the domain of the service
# host: api.datascience.ch
# array of all schemes that your API supports
schemes:
  - https
# will be prefixed to all paths
basePath: /
produces:
  - application/json
consumes:
  - application/json
paths:
  '/mutation':
    $ref: ../paths/graph-mutation.yaml
  '/mutation/create_vertex':
    $ref: ../paths/graph-mutation-create-vertex.yaml
definitions:
  $ref: ./definitions.yaml#/definitions
