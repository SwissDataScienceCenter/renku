# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

POST    /deploy                 controllers.DeployController.deploy
POST    /undeploy               controllers.DeployController.undeploy
GET     /status                 controllers.DeployController.status
GET     /list                 controllers.DeployController.list
POST    /:id/register           controllers.DeployController.register(id: Long)

