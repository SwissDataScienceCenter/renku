apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-hostname
  labels:
    app: {{ template "renku.name" . }}
    chart: {{ template "renku.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  KC_HOSTNAME: "https://{{ .Values.global.renku.domain }}/auth"
