# in order to use defaults from the file copy it as tests-defaults.conf and fill in the missing values.

env = "https://dev.renku.ch"
email = ""
username = ""
password = ""
fullname = ""
renku-version = "0.10.4"
renku-install-command = "python3 -m pip install 'renku==%s'"
