branches:
  only:
  - master

language: python

python:
- '3.6'

addons:
  apt:
    packages:
    - python-docutils
    - graphviz

script:
- make uml

deploy:
- provider: script
  skip_cleanup: true
  script: bash ./.travis/git-commit-build.sh
  on:
    branch: master

